{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | grep -q '/notes/.*\\.md$' && npm run build:html --silent >/dev/null 2>&1 || true"
          }
        ]
      }
    ]
  }
}
